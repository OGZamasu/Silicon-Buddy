<?xml version="1.0" encoding="utf-8"?>
<!--
  The Mac's control API is plain HTTP on a private address: 10.0.2.2 from the emulator,
  a 100.64/10 tailnet address on a phone. Nothing here crosses the public internet, and
  there is no certificate to validate. Everything else stays HTTPS-only.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
